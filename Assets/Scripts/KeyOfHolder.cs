using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class KeyOfHolder : MonoBehaviour
{
    // Start is called before the first frame update

    public string key;

    void Start()
    {
   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
